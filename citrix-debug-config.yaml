# Debug configuration with minimal probes for testing
agent:
  key: "60b8b7f2-9f8c-4f41-b9e0-46028baf8dc4"
  mode: offline
  generated: true

auto_update:
  enabled: false

cache:
  retention_minutes: 5

storage:
  - name: http
    params:
      port: 8080
      bind_address: "127.0.0.1"
      endpoints: ["prtg", "web", "nagios"]

probes:
  # Only Citrix probe for debugging
  - name: citrix
    params:
      base_url: "https://director.noble-age.fr/Citrix/Monitor/OData/v4/Data"
      environment: "PROD"
      interval: 300  # 5 minutes for debugging
      auth:
        method: "ntlm"
        username: "noble-age.fr\\svc_api_sensor"
        password: "gs-+8L<2:mL38]17"
      tls:
        verify_ssl: false
      timeout: 60     # Longer timeout for debugging
      retry:
        max_attempts: 1  # No retries for cleaner logs
        backoff_factor: 1