# Test configuration with Basic Auth for debugging
agent:
  key: "60b8b7f2-9f8c-4f41-b9e0-46028baf8dc4"
  mode: offline
  generated: true

auto_update:
  enabled: false
  url: "https://eu-west-1.intake.senhub.io/releases"

cache:
  retention_minutes: 5

storage:
  - name: http
    params:
      port: 8080
      bind_address: "127.0.0.1"
      endpoints: ["prtg", "web", "nagios"]

probes:
  # System monitoring (minimal for testing)
  # Citrix monitoring with Basic Auth for testing
  - name: citrix
    params:
      base_url: "https://director.noble-age.fr/Citrix/Monitor/OData/v4/Data"
      environment: "PROD"
      interval: 120
      auth:
        method: "basic"  # Changed to basic for testing
        username: "noble-age.fr\\svc_api_sensor"
        password: "gs-+8L<2:mL38]17"
      tls:
        verify_ssl: false
      timeout: 30
      retry:
        max_attempts: 3
        backoff_factor: 2
