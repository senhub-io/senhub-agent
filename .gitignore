# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

/tmp/
/senhub-agent_darwin_amd64
/senhub-agent_windows_amd64.exe
/senhub-agent_linux_amd64
/senhub-agent_linux_arm64

# env file
.env

/*.log
/dist
.air.toml

**/.claude/settings.local.json

# Security - Exclude certificate files
*.pem
*.key
*.crt
**/certs/
/certs/

# License system - Exclude test/production keys and licenses
license-private-key.pem
license-public-key.pem
senhub-license-private-key.pem
senhub-license-public-key.pem
example-license-*.jwt
license-*.jwt

# Production keys and license testing (CRITICAL - NEVER COMMIT)
.keys/
test_configs/
token_*.txt
config_test_license*.yaml

# Coverage reports
coverage.html
coverage.out

# Compiled executables in root
/agent
/agent.exe

# Test installation directory
/test-install/

# Claude Code - Personal documentation and scripts
scripts/Claude/

# Local audit / migration notes (POC audits, OSS migration runbook,
# issues triage). Drafted in-tree for convenience but must never reach
# a public mirror — they reference customers, infra, and commercial
# decisions.
docs/audit/

# E2E Testing Infrastructure (Playwright)
node_modules/
package.json
package-lock.json
playwright.config.js
playwright-report/
test-results/
tests/
.playwright/

# E2E Test scripts and configuration
scripts/run-e2e-tests.sh
config_demo_web.yaml

# E2E Test automation agents
.claude/agents/e2e-test-runner.md
.claude/agents/test-runner.md

# Generated Word documentation
docs/user-guide/word/
docs/user-guide/**/*.docx
docs/user-guide/.temp-word-gen/

# Hugo generated site
docs/user-guide/public/
docs/user-guide/resources/

# Playwright MCP logs
**/.playwright-mcp/

# Claude Code - Local hookify rules
.claude/hookify.*.local.md

# IBM i bridge — locally compiled native runner (per-arch, opt-in
# alternative to `java -cp jt400.jar:. Jt400Runner`).
internal/agent/probes/ibmi/bridge/jt400runner

# Claude Code scheduled wake-up lock (runtime artifact)
.claude/scheduled_tasks.lock
